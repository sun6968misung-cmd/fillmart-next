# 필마트 홈페이지 QA 보고서
검수일: 2026-09-08 | 검수자: 검수팀장 | 기준 브랜치: feature/pilmart-nextjs-shadcn

---

## 결과: 수정 필요

---

### frontend가 고칠 것

#### [1. 자동검사] ESLint — 17개 오류

> `pnpm lint` 종료 코드 1 (빌드는 통과하나 린트는 실패)

- [ ] `context/StoreProvider.tsx:26` — `useEffect` 내부에서 `setItems()` 직접 호출 (`react-hooks/set-state-in-effect`) — 초기화 로직을 `useState` 이니셜라이저 함수로 이동: `useState(() => lsGet(...))`
- [ ] `context/StoreProvider.tsx:118` — 위시리스트 `setIds()` 동일 패턴 — 위와 같이 수정
- [ ] `context/StoreProvider.tsx:158` — 인증 `setUser()` 동일 패턴 — 위와 같이 수정
- [ ] `context/StoreProvider.tsx:29` — `persist` 변수 정의 후 미사용 (`@typescript-eslint/no-unused-vars`) — 삭제 또는 실제 사용
- [ ] `components/home/FlashSaleSection.tsx:21` — `setConfig()` 동일 패턴 — 이니셜라이저 함수로 이동
- [ ] `components/home/NoticePreview.tsx:23` — `setNotices()` 동일 패턴 — 이니셜라이저 함수로 이동
- [ ] `components/home/StoreInfoBar.tsx:29` — `setInfo()` 동일 패턴 — 이니셜라이저 함수로 이동
- [ ] `components/layout/Footer.tsx:20` — `setInfo()` 동일 패턴 — 이니셜라이저 함수로 이동
- [ ] `components/home/HeroBanner.tsx:127-129` — render 도중 `containerRef.current` 접근 (`react-hooks/refs`) — `useEffect + useState(SLIDE_W)` 로 containerW를 상태로 분리하고 ResizeObserver로 갱신
- [ ] `app/admin/page.tsx:76-78` — `setOrders`, `setProducts`, `setNotices`, `setStoreInfo` 동일 패턴 — 이니셜라이저 함수로 이동
- [ ] `app/admin/page.tsx:81` — `useEffect` 의존성 배열에서 `DEFAULT_FLASH` 누락 (`react-hooks/exhaustive-deps`) — 의존성 추가 또는 `useRef`로 분리
- [ ] `app/notice/page.tsx:10` — `setNotices()` 동일 패턴 — 이니셜라이저 함수로 이동
- [ ] `app/orders/page.tsx:14` — `setOrders()` 동일 패턴 — 이니셜라이저 함수로 이동
- [ ] `app/page.tsx:44-46` — 카테고리 변경 시 `setSort`, `setPriceIdx`, `setOnlyDiscount` — `useEffect` 내 setState는 "derived state" 안티패턴; `cat`이 바뀌면 키(key)를 바꾸거나 `useMemo` 내에서 default 처리
- [ ] `app/page.tsx:74` — `useMemo` 의존성 배열에 불필요한 `cat` 포함 (`react-hooks/exhaustive-deps`) — 제거
- [ ] `app/product/[id]/page.tsx:58` — `setIsAdmin()` 동일 패턴 — 이니셜라이저 함수로 이동
- [ ] `app/checkout/page.tsx:59` — `useMemo(() => `pilmart_${Date.now()}`, [])` — render 중 불순 함수 호출 (`react-hooks/purity`) — `useRef`로 교체: `const orderIdRef = useRef(\`pilmart_${Date.now()}\`)`
- [ ] `app/checkout/page.tsx:35` — `clearCart` 비구조화 후 미사용 (`@typescript-eslint/no-unused-vars`) — 제거

#### [1. 자동검사] ESLint — 16개 경고 (`<img>` → `<Image />`)

- [ ] `components/home/HeroBanner.tsx:159` — `<img>` 사용 (`@next/next/no-img-element`) — 외부 URL(Unsplash)이므로 `next.config.ts`에 `images.remotePatterns` 추가 후 `<Image />` 교체
- [ ] `components/home/FlashSaleSection.tsx:70` — 위와 동일
- [ ] `components/layout/Navbar.tsx:112` — `/logo.png` (`<img>`) — `<Image />` 로 교체
- [ ] `components/product/ProductCard.tsx:31` — 상품 썸네일 (`<img>`) — `<Image />` 교체
- [ ] `app/admin/page.tsx:633,1066` — 관리자 상품 미리보기 이미지 2곳 — `<Image />` 또는 허용된 도메인 등록
- [ ] `app/cart/page.tsx:59` — 장바구니 썸네일 — `<Image />` 교체
- [ ] `app/flash-product/[idx]/page.tsx:63` — 특가 상품 이미지 — `<Image />` 교체
- [ ] `app/product/[id]/page.tsx:155,258,384,432` — 상품 상세 이미지 4곳 — `<Image />` 교체

#### [2. 정보 정확성] brief.md에 없는데 화면에 표시된 내용

> brief.md가 전부 비어 있음. 아래 항목은 코드에 하드코딩된 구체적 사실이므로 대표가 확인 전까지 삭제 또는 "[확인 필요]" 처리 필요.

- [ ] `app/faq/page.tsx:6` — "오전 11시 이전 주문 시 당일 오후 배송" — 확인 안 된 배송 조건; 삭제 또는 대표 확인 후 적용
- [ ] `app/faq/page.tsx:8` — "회원 가입 시 첫 주문 무료배송 혜택" — 확인 안 된 혜택; 삭제 또는 대표 확인 후 적용
- [ ] `app/faq/page.tsx:4` — "도서산간 지역 추가 배송비" — 배송 정책 미확정; 확인 필요
- [ ] `components/home/HeroBanner.tsx:57` + `lib/categoryConfig.ts:59` + `lib/products.ts:29` — "충남 당진 농협 직송 햇쌀" — 실제 납품처 확인 전 노출 금지
- [ ] `app/success/page.tsx:48` — "오전 주문 시 당일 오후 배송됩니다" — 배송 조건 미확인
- [ ] `components/layout/Navbar.tsx:105` — "🚚 오전 주문 시 당일 배송 · 신선식품 직송" — 배송 조건 미확인; 노출 전 대표 승인 필요

#### [3. 모바일] 터치 타깃 44px 미만

- [ ] `components/product/ProductCard.tsx:49` — 찜(하트) 버튼 `w-7 h-7` = 28px — 최소 `w-11 h-11`(44px)로 확대. 또는 투명한 `min-h-11 min-w-11` 영역 추가
- [ ] `app/cart/page.tsx:105-110` — 삭제 버튼 `p-1`만 있고 사이즈 미지정 (`h-4 w-4` 아이콘 기준 약 24px) — `p-2.5` 이상 또는 `h-11 w-11` 지정

#### [3. 모바일] 잠재적 가로 스크롤

- [ ] `components/home/HeroBanner.tsx:127-129` — render 중 `containerRef.current.offsetWidth` 읽기: SSR 시 `offsetWidth = undefined → SLIDE_W(fallback)` 이므로 첫 렌더에서 slide width가 틀릴 수 있고 좁은 화면에서 가로 overflow 발생 가능 — lint 이슈와 함께 `useEffect`로 이동 후 수정

#### [3. 모바일] 검색 입력 — label 없음

- [ ] `components/layout/Navbar.tsx:117-123` — `<input type="text">` 에 `<label>` 또는 `aria-label` 없음 — `aria-label="상품 검색"` 추가

#### [4. 접근성]

- [ ] `components/product/ProductCard.tsx:47-52` — 찜 버튼에 `aria-label` 없음 — `aria-label={wished ? '찜 해제' : '찜하기'}` 추가
- [ ] `app/product/[id]/page.tsx:155` — 관리자 미리보기 이미지 `alt=""` (장식이 아님, 사용자가 업로드한 이미지) — `alt="상품 이미지 미리보기"` 등 의미 있는 alt 추가
- [ ] `components/layout/Navbar.tsx:178` — `text-red-500` / `hover:text-red-600` 사용 (토큰 외 임의 색상) — `text-sale hover:text-sale/80` 또는 primary variant 사용

#### [4. 접근성] 키보드 포커스

- [ ] `components/layout/Navbar.tsx:124` — 검색 제출 `<button>` (`type="submit"`)에 `aria-label` 없음 — `aria-label="검색"` 추가

#### [5. SEO/공유]

- [ ] `app/layout.tsx:12-15` — `metadata` 에 `openGraph` 미지정 (og:image, og:title, og:description 없음) — 카카오/라인 공유 시 미리보기 이미지 없음; og 이미지 파일 추가 후 layout.tsx에 `openGraph` 블록 추가
- [ ] 프로젝트 전체 — `app/sitemap.ts` 파일 없음 — Next.js App Router `sitemap.ts` 추가 (구글 크롤 효율)
- [ ] 프로젝트 전체 — `app/robots.ts` 파일 없음 — `robots.ts` 추가
- [ ] 서브 페이지 전체 — `export const metadata` 선언 없음 (layout.tsx 하나만 존재) — 각 페이지(`/product/[id]`, `/category/[slug]`, `/faq`, `/notice` 등)에 개별 `metadata` 또는 `generateMetadata` 추가

#### [6. 개인정보]

- [ ] `app/auth/page.tsx:160` — 회원가입 폼에 개인정보처리방침 동의 체크박스 없음 — 가입 전 "이용약관 및 개인정보처리방침 동의" 체크박스 필수 추가 (법적 의무)
- [ ] `app/checkout/page.tsx` — 배송지 수집 시 "주문 처리 목적으로 개인정보를 수집합니다" 안내 문구 없음 — 체크아웃 상단에 안내 추가

#### [7. 디자인 일관성] 토큰 외 임의 hex 색상 (globals.css·tokens.md 외부 파일)

> `globals.css`의 `--slide-*` 토큰 블록과 완전히 동일한 값이 `lib/categoryConfig.ts`에 hex로 중복 하드코딩되어 있음.

- [ ] `lib/categoryConfig.ts:16,21,26,31,36,41,46,51,56,61` — `bg`, `badgeColor` 필드에 `#f5ede0`, `#eb3800`, `#fef3e2`, `#d97706`, `#fce8e6`, `#dc2626`, `#e0f0f8`, `#0284c7`, `#e8f5e9`, `#16a34a` 하드코딩 — globals.css의 `--slide-*-bg`, `--slide-*-badge` CSS 변수를 참조하도록 리팩터링하거나, 카테고리 config에서 bg/badgeColor 필드를 제거하고 CSS 변수명만 저장
- [ ] `app/admin/page.tsx:268-281` — 인쇄용 HTML 인라인 스타일에 `#f5f5f5`, `#555`, `#ddd`, `#f0f0f0`, `#c53030`, `#ebf8ff`, `#2b6cb0` 사용 — 인쇄 뷰 전용이므로 우선순위 낮음; 향후 print.css로 분리 권장

---

### designer에게 확인

- [ ] `components/layout/Navbar.tsx:178` — "이번주특가" 링크가 `text-red-500`으로 별도 스타일 — `text-sale` 또는 `text-primary` 토큰 중 어느 것으로 통일할지 결정 요청
- [ ] `components/home/HeroBanner.tsx:176` — 슬라이드 제목 `text-[2rem] text-gray-900` — 밝은 배경 위 dark 텍스트이므로 대비 문제 없으나, 토큰 체계의 `text-foreground` 대신 `text-gray-900` 사용 중 — 통일 여부 결정 요청

---

### 사용자(대표) 확인 필요

> 아래 항목은 brief.md에 없어서 코드에서 지어낸 내용입니다. 사실이면 brief.md를 채워주세요. 사실이 아니면 삭제해 드립니다.

- [ ] brief.md 미입력: 상모점·다사점 전화번호, 주소, 영업시간, 휴무일 (StoreInfoBar·Footer가 "[확인 필요]"로 표시 중)
- [ ] "오전 주문 시 당일 배송" — 네이버 공지바·성공 페이지·FAQ에 노출 중. 실제 마감 시간(오전 11시)·배송 보장 여부 확인
- [ ] "첫 주문 무료배송" — FAQ에 노출 중. 현재 운영 중인 혜택인지 확인
- [ ] "충남 당진 농협 직송 햇쌀" — 배너·카테고리 페이지·상품 설명에 노출 중. 실제 납품처 맞는지 확인
- [ ] "전국 배송 가능, 도서산간 추가배송비" — FAQ·상품상세에 노출 중. 실제 배송 범위 확인
- [ ] 필통치킨 — brief.md에 "홈페이지에 넣을지 여부" 미기입. 현재 홈페이지에는 없음. 추가 여부 결정 요청

---

## 자동검사 요약

| 항목 | 결과 |
|---|---|
| `pnpm tsc --noEmit` | ✅ 오류 없음 |
| `pnpm build` | ✅ 성공 (18개 페이지 생성) |
| `pnpm lint` | ❌ 17개 오류, 16개 경고 (종료 코드 1) |

**주요 오류 파일:** `context/StoreProvider.tsx`, `components/home/HeroBanner.tsx`, `app/checkout/page.tsx`, `app/admin/page.tsx`, `app/page.tsx`, `app/product/[id]/page.tsx`, `app/notice/page.tsx`, `app/orders/page.tsx`, `components/home/FlashSaleSection.tsx`, `components/home/NoticePreview.tsx`, `components/home/StoreInfoBar.tsx`, `components/layout/Footer.tsx`
